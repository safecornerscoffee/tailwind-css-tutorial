{
  "name": "tailwind-css-tutorial",
  "version": "1.0.0",
  "description": "tailswind css tutorial",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss build -i main.css -o tailwind.css",
    "watch": "npx tailwindcss build -i main.css -o tailwind.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "autoprefixer": "^10.3.1",
    "tailwindcss": "^2.2.7"
  }
}
